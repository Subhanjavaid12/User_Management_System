<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management System - Register</title>
    
    <style>
        /* This CSS is copied from your login page to ensure the themes match */
        * {
            box-sizing: border-box;
        }
        
        html, body { 
            height: 100%; 
            margin: 0; 
            padding: 0; 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }
        
        body { 
            display: flex; 
            justify-content: center; 
            align-items: center; 
            right: 25%;
            width: 200%;
            height: 200%;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            height: 100%; 
            position: relative;
            overflow-x: auto; 
        }
        
        body::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(59, 130, 246, 0.1) 0%, transparent 50%);
            animation: float 20s ease-in-out infinite;
        }
        
        body::after {
            content: '';
            position: absolute;
            bottom: -50%;
            right: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(147, 51, 234, 0.1) 0%, transparent 50%);
            animation: float 25s ease-in-out infinite reverse;
        }
        
        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
        }
        
        .register-card { 
            width: 480px; /* Slightly wider to accommodate more fields */
            max-width: 90vw;
            background: rgba(30, 41, 59, 0.95);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(148, 163, 184, 0.1);
            box-shadow: 
                0 25px 50px -12px rgba(0, 0, 0, 0.5),
                0 0 0 1px rgba(255, 255, 255, 0.05);
            border-radius: 1.5rem;
            display: flex; 
            flex-direction: column; 
            gap: 24px; 
            padding: 48px 32px;
            position: relative;
            z-index: 10;
            margin: 40px 0; /* Add margin for smaller screens */
        }
        
        .card-header { 
            display: flex; 
            flex-direction: column; 
            align-items: center; 
            gap: 12px;
            margin-bottom: 8px;
        }
        
        .card-header::before {
            content: '📝'; /* Changed icon for registration */
            font-size: 48px;
            margin-bottom: 8px;
            padding: 16px;
            background: linear-gradient(135deg, #10b981, #3b82f6); /* Green to blue gradient */
            border-radius: 50%;
            width: 80px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 10px 25px rgba(16, 185, 129, 0.3);
        }
        
        .card-header b { 
            color: #f8fafc; 
            font-size: 28px; 
            font-weight: 600; 
            margin-bottom: 8px;
            text-align: center;
            background: linear-gradient(135deg, #f8fafc, #cbd5e1);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }
        
        .card-header p {
            color: #94a3b8;
            font-size: 14px;
            margin: 0;
            text-align: center;
        }
        
        .card-body { 
            display: flex; 
            flex-direction: column; 
            align-items: center;
        }
        
        form { 
            display: flex; 
            flex-direction: column; 
            gap: 16px; /* Adjusted gap for more fields */
            width: 100%;
            max-width: 360px;
        }
        
        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }
        
        form label { 
            font-size: 14px; 
            font-weight: 600; 
            text-align: left; 
            line-height: 1.5; 
            color: #e2e8f0;
        }
        
        form input, form textarea { 
            height: 48px; 
            border: 1px solid rgba(148, 163, 184, 0.2);
            background: rgba(15, 23, 42, 0.6);
            border-radius: 0.75rem; 
            padding: 0 16px; 
            font-size: 15px;
            color: #f8fafc;
            transition: all 0.3s ease;
            backdrop-filter: blur(10px);
        }

        form textarea {
            height: auto; /* Allow textarea to grow */
            padding: 12px 16px;
        }
        
        form input::placeholder, form textarea::placeholder {
            color: #64748b;
        }
        
        form input:focus, form textarea:focus {
            outline: none;
            border-color: #10b981;
            box-shadow: 
                0 0 0 3px rgba(16, 185, 129, 0.1),
                0 4px 12px rgba(16, 185, 129, 0.15);
            background: rgba(15, 23, 42, 0.8);
        }
        
        form button { 
            width: 100%; 
            height: 52px; 
            background: linear-gradient(135deg, #10b981, #059669);
            color: white; 
            border: none; 
            border-radius: 0.75rem; 
            font-size: 16px; 
            font-weight: 600; 
            cursor: pointer; 
            margin-top: 12px;
            transition: all 0.3s ease;
        }
        
        form button:hover {
            background: linear-gradient(135deg, #059669, #047857);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(16, 185, 129, 0.4);
        }
        
        .card-footer { 
            text-align: center; 
            margin-top: 8px;
        }
        
        .card-footer p {
            margin: 0;
            color: #94a3b8;
            font-size: 14px;
        }
        
        .card-footer a { 
            text-decoration: none; 
            color: #3b82f6;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .card-footer a:hover { 
            color: #60a5fa;
            text-decoration: underline;
        }
        
        .error-message { 
            color: #fca5a5;
            background: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            padding: 16px;
            border-radius: 0.75rem;
            margin-bottom: 20px;
            text-align: center;
            font-size: 14px;
            font-weight: 500;
            backdrop-filter: blur(10px);
            width: 100%;
        }
    </style>
</head>

<body>
    <div class="register-card">
        <div class="card-header">
            <b>Create an Account</b>
            <p>Join the User Management System</p>
        </div>
        <div class="card-body">
            {{#if message}}
                <div class="error-message">
                    {{message}}
                </div>
            {{/if}}

            <form action="/register" method="POST">
                <div class="form-group">
                    <label for="first_name">First Name</label>
                    <input id="first_name" type="text" name="first_name" placeholder="Enter your first name" required>
                </div>
                
                <div class="form-group">
                    <label for="last_name">Last Name</label>
                    <input id="last_name" type="text" name="last_name" placeholder="Enter your last name" required>
                </div>

                <div class="form-group">
                    <label for="email">Email Address</label>
                    <input id="email" type="email" name="email" placeholder="Enter your email" required>
                </div>
                
                <div class="form-group">
                    <label for="phone">Phone</label>
                    <input id="phone" type="tel" name="phone" placeholder="Enter your phone number" required>
                </div>

                <div class="form-group">
                    <label for="password">Password</label>
                    <input id="password" type="password" name="password" placeholder="Create a password (min. 8 characters)" required minlength="8">
                </div>

                <div class="form-group">
                    <label for="comments">Comments (Optional)</label>
                    <textarea id="comments" name="comments" rows="3" placeholder="Any additional comments..."></textarea>
                </div>
                
                <button type="submit">Create Account</button>
            </form>
        </div>
        <div class="card-footer">
            <p>Already have an account? <a href="/">Sign In</a></p>
        </div>
    </div>
</body>
</html>
