<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <style>
        * { box-sizing: border-box; }
        html, body { height: 100%; margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif; }
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            left: 24%;
         
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            min-height: 100vh;
            position: relative;
            overflow-y: auto;
            padding: 20px;
        }
        .card { width: 100%; max-width: 480px; background: rgba(30, 41, 59, 0.95); backdrop-filter: blur(20px); border: 1px solid rgba(148, 163, 184, 0.1); box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5); border-radius: 1.5rem; display: flex; flex-direction: column; gap: 24px; padding: 48px 32px; position: relative; z-index: 10; }
        .card-header { text-align: center; margin-bottom: 8px; }
        .card-header b { color: #f8fafc; font-size: 28px; font-weight: 600; }
        .card-body { display: flex; flex-direction: column; align-items: center; width: 100%; }
        form { display: flex; flex-direction: column; gap: 20px; width: 100%; max-width: 320px; }
        .form-group { display: flex; flex-direction: column; gap: 8px; }
        form label { font-size: 16px; font-weight: 600; color: #e2e8f0; }
        form input { height: 48px; border: 1px solid rgba(148, 163, 184, 0.2); background: rgba(15, 23, 42, 0.6); border-radius: 0.75rem; padding: 0 16px; font-size: 15px; color: #f8fafc; }
        form button { width: 100%; height: 52px; background: linear-gradient(135deg, #10b981, #059669); color: white; border: none; border-radius: 0.75rem; font-size: 16px; font-weight: 600; cursor: pointer; margin-top: 12px; }
        .error-message { color: #fca5a5; background: rgba(239, 68, 68, 0.1); border: 1px solid rgba(239, 68, 68, 0.2); padding: 16px; border-radius: 0.75rem; margin-bottom: 20px; text-align: center; font-size: 14px; font-weight: 500; width: 100%; }
        .success-message { color: #86efac; background: rgba(34, 197, 94, 0.1); border: 1px solid rgba(34, 197, 94, 0.2); padding: 16px; border-radius: 0.75rem; margin-bottom: 20px; text-align: center; font-size: 14px; font-weight: 500; width: 100%; }
    </style>
</head>
<body>
    <div class="card">
        <div class="card-header">
            <b>Change Your Password</b>
        </div>
        <div class="card-body">
            
            <!-- This Handlebars block will only render if an 'error' variable is passed from the server -->
            {{#if error}}
                <div class="error-message">
                    {{error}}
                </div>
            {{/if}}
            
            <!-- This block will only render if a 'message' variable is passed -->
            {{#if message}}
                <div class="success-message">
                    {{message}}
                </div>
            {{/if}}

            <form action="/new-password/{{id}}" method="POST">
             
                <div class="form-group">
                    <label for="newPassword">New Password</label>
                    <input id="newPassword" type="password" name="password" placeholder="Enter new password" required minlength="8">
                </div>
                <div class="form-group">
                    <label for="confirmNewPassword">Confirm New Password</label>
                    <input id="confirmNewPassword" type="password" name="confirmPassword" placeholder="Confirm new password" required>
                </div>
                <button type="submit">Update Password</button>
            </form>
        </div>
    </div>
</body>
</html>
